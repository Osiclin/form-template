export default function FilterBox(props) {
    return(
        <div>
            <form>
                <legend>hey</legend>
                <select name={props.selectName} >
                    <option value=""></option>
                </select>
            </form>
        </div>
    )
}